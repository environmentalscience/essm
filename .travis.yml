notifications:
  email: false

language: python

python:
  - '2.7'

sudo: true

addons:
  apt:
    sources:
      - "ppa:aims/sagemath"
    packages:
      - "sagemath-upstream-binary"

cache:
  apt: true
  pip: true

install:
  - sudo sage setup.py install

script: sage setup.py test
