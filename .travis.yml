notifications:
  email: false

language: python

python:
  - '2.7'

sudo: true

before_install:
  - sudo apt-add-repository -y ppa:aims/sagemath
  - sudo apt-get update
  - sudo apt-get install sagemath-upstream-binary

install:
  - sudo sage setup.py install

script: sage setup.py test
